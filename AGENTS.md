# AGENTS.md

## STOP — this template is not set up yet

This is an unconfigured clone of the CEN Starter template: `.template/` exists and
`package.json` → `cen.finalized` is `false`.

Whatever the user asked for — even a concrete feature like "build a todo list" — the first
step is setup. Read `.agents/skills/setup/SKILL.md` and follow it completely: confirm the
project name and location, interview the user, bootstrap the chosen configuration, verify
the running app, and finalize with the user's explicit approval. A feature request never
overrides this gate.

Do not add routes, tables, migrations, or pages before finalization. Finalization replaces
this file with the project's real working guide and activates the feature skills.

Maintaining the CEN Starter template itself, not starting a project from it? Use
`.agents/skills/template-maintenance/SKILL.md`; the post-setup working conventions live in
`.template/scaffold/AGENTS.md`.
