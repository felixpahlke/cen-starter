apiVersion: v1
kind: Service
metadata:
  name: cen-starter
  labels:
    app.kubernetes.io/name: cen-starter
spec:
  selector:
    app.kubernetes.io/name: cen-starter
  ports:
    - name: http
      port: 8080
      targetPort: http
